// Palette values needed in JS (the SVG charts set stroke/fill colours here).
// The full design system lives as CSS variables in index.css.
export const COLORS = {
  border: "#d6d9de",
  muted: "#6b7280",
  accent: "#2f6fed",
  green: "#2e7d32",
};
